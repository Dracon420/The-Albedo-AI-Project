chromadb>=0.5.0
opencv-python>=4.10.0
customtkinter>=5.2.0
Pillow>=10.0.0
ddgs>=1.0.0
open-interpreter>=0.3.17
python-dotenv>=1.0.0
sentence-transformers>=3.0.0
vosk>=0.3.45
requests>=2.31.0

# Audio stack
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.26.0
scipy>=1.13.0

# Kokoro TTS (Phase 4 — opt-in via AUDIO_TTS=kokoro; Piper remains the default)
# Model files (kokoro-v1.0.onnx + voices-v1.0.bin, ~340 MB total) are
# downloaded by setup_utility.py from github.com/thewh1teagle/kokoro-onnx/releases
kokoro-onnx>=0.5.0

# STT engines (Phase 4 N+2 — opt-in via AUDIO_STT=deepgram|whisper; Vosk remains default)
# faster-whisper provides the distil-small.en offline fallback. Already listed
# under the install set above (not re-pinned here to avoid version drift).
faster-whisper>=1.0.0
# Deepgram SDK is the cloud primary; only consulted when DEEPGRAM_API_KEY is set.
deepgram-sdk>=3.7.0

# FastAPI server (Tailscale bridge for mobile client)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Web scraping (used by Open Interpreter code generation + deep content extraction)
playwright>=1.44.0
beautifulsoup4>=4.12.0
trafilatura>=1.10.0
httpx>=0.27.0
lxml>=5.2.0

# Hardware telemetry (tactical audit, system vitals)
psutil>=5.9.0
WMI>=1.5.1
GPUtil>=1.4.0

# Obsidian Vault RAG (chromadb + sentence-transformers already above)
langchain>=0.2.0
langchain-community>=0.2.0

# Swarm Matrix -- multi-agent cloud LLM APIs
google-genai>=0.3.0
groq>=0.9.0
together>=1.2.0

# TTS — Edge-TTS (primary, cloud) + Piper (fallback, offline)
edge-tts>=6.1.9
